import * as React from "react";
import { UploadImage } from "../../components/UploadImage/UploadImage";

interface Props {}

export const Heritage: React.FunctionComponent<Props> = ({}) => {
  return (
    <div>
      <UploadImage />
    </div>
  );
};
